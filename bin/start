#!/usr/bin/env bash
[[ "$TRACE" ]] && set -x
set -eu -o pipefail

source .env

docker run \
	--name="$AMPACHE_SERVER_CONTAINER" \
	--volume "$AMPACHE_MUSIC_DIR":/media:ro \
	--publish "$AMPACHE_SERVER_PORT":80 \
	--detach \
	ampache/ampache
